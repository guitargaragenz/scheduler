---
doc_status: closed
---

# Handoff — 8 August 2026

## What got done last session

1. **Wrote down "smart zone / dumb zone" properly.** It had never actually been recorded —
   recovered the meaning from July session transcripts. Now a memory file
   (`smart-zone-dumb-zone.md`) in the memory folder that actually loads, indexed in MEMORY.md.
   `sz` typed on its own = "are we still in the smart zone?" — answer with the real context
   percentage and a straight verdict, never reassurance.

2. **Made Trevor's personal instructions global.** They used to only exist inside this project.
   - Canonical copy: `personal-instructions.md` at repo root (commit `95581b5`, pushed).
   - `~/.claude/CLAUDE.md` is a symlink to it, so it loads in **every** session on Micky,
     whatever repo he's in.
   - Covers: short answers as an accessibility requirement, plain English, straight verdicts,
     and the two shorthands `sz` and `tt` ("too technical" — re-say it plainer, don't ask which
     part lost him).
   - **Gap:** Claude Code *web* sessions on a different repo still won't see it. No fix short of
     pasting a copy into that repo.

## Open, not started

- **Moby is still not set up** — it has never cloned the repo. Reminder was set for 9pm on
  7 Aug, so it has already passed and needs re-setting. The four steps are in the reminder's
  notes: clone → `npm install` → symlink `~/.claude/CLAUDE.md` → AirDrop `.env` from Micky.
  **The `.env` keys must be AirDropped, never pasted into a chat** — a chat saves them to the
  transcript.

## Offered and never accepted (don't start without asking)

- Merge the stale iCloud-path memory folder (36 files, last touched 24 July) into the live one
  (7 files). They got orphaned when the project folder moved.
- Fix the CLAUDE.md device list so it says Moby is unconfigured.
- Fix stale line 30 of `SCHEDULER-ARCHITECTURE.md` — it still says PJ comes from the Google
  Sheet; PJ is set in the app now.

## Scheduled separately

Sunday board meeting — Mac only, fresh session, plain `/sunday-board-meeting`. He asked that the
16 dead jobs not be raised there.
