# Scope lock

Nothing approved. The "no job ever has no bench" build shipped at `b10d52f`
on 2026-09-11.

Next up, not yet briefed or approved:
- Strip the dead "No bench set" groups from `BenchBoardPage`, `BenchWeekPage`,
  `WeeklySummaryModal`, `JobShelf`. They go quietly empty now.
