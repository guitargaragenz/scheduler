# Scope lock

Empty. Nothing is approved to build.

The completion-date fix shipped 2026-09-11 at `049ec99`.

Next piece of work is chosen from Parked, with Trevor. See `docs/briefs/README.md`
for what is live and what has only been noticed.

## Approved 2026-09-11 — rename only

`DailyLogPage.jsx` → `DayViewPage.jsx`. It is the board's Day View, not the Daily
Log, and the clashing name caused two misreads of the money code in one session.
Rename and reference updates only. No behaviour change. Old docs left as history.
